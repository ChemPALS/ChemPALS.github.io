# Git ignore tells git to ignore certain files and directories. This is useful for files that are generated by the build process or are specific to your development environment.

# For example, you can ignore the .idea directory that is created by PyCharm.
.idea/